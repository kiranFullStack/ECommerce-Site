import React from "react"
import "./App.scss"
import Button from "@material-ui/core/Button"

function App() {
  return (
    <div>
      <h1>
        <i class="material-icons">shopping_cart</i> Ecom App for Target with
        React Hooks, Redux, Material UI, SCSS and Axios
      </h1>
      <Button variant="contained" color="primary">
        Hello World
      </Button>
    </div>
  )
}

export default App
